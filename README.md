# library-management-sql
A simple SQL-based Library Management Database to manage members, books, and issue records. Includes modules to create tables, insert data, issue books, and update returns using joins and constraints. Demonstrates relational design, normalization, and key SQL operations.
